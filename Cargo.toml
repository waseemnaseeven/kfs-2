[package]
name = "kfs"
version = "0.1.0"
edition = "2021"

[lib]
path = "./src/lib.rs"
crate-type = ["staticlib"]

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
lto = true
codegen-units = 1
strip = "symbols"
opt-level = "z"